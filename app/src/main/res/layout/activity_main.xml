<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start FPS Measure"
        android:id="@+id/main_activity_start_button"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="114dp"
        android:onClick="startFPS" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop FPS Measure"
        android:id="@+id/main_activity_stop_button"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/main_activity_start_button"
        android:layout_alignStart="@+id/main_activity_start_button"
        android:layout_alignRight="@+id/main_activity_start_button"
        android:layout_alignEnd="@+id/main_activity_start_button"
        android:onClick="stopFPS" />
</RelativeLayout>
